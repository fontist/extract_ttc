# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: rake

permissions:
  contents: read

on:
  push:
    branches: [master, main]
    tags: [v*]
  pull_request:

jobs:
  rake:
    uses: metanorma/ci/.github/workflows/generic-rake.yml@main
    secrets:
      pat_token: ${{ secrets.FONTIST_CI_PAT_TOKEN }}
